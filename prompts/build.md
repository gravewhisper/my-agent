---
description: Implement the next change incrementally with verification
---
Follow `incremental-implementation` and `test-driven-development` for: $ARGUMENTS

Work in thin slices, inspect the codebase before changing it, and verify each increment.
